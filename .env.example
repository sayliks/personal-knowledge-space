# Database (Supabase)
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# Auth.js
AUTH_SECRET="your-secret-at-least-32-chars"
AUTH_URL="http://localhost:3000"

# GitHub OAuth (optional — for comment section sign-in)
# Create at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github
# AUTH_GITHUB_ID=
# AUTH_GITHUB_SECRET=

# Admin seed
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="changeme123"

# --- Analytics geolocation (optional) ---
# Uses deployment geolocation headers first. When city data is missing, public
# visitor IPs are looked up via ipapi.co. Set to "false" to disable that fallback.
ANALYTICS_GEOLOOKUP="true"
